function bytesToB64(bytes){let s="";for(const b of bytes)s+=String.fromCharCode(b);return btoa(s).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"")}
function b64ToBytes(s){s=s.replace(/-/g,"+").replace(/_/g,"/");while(s.length%4)s+="=";return Uint8Array.from(atob(s),c=>c.charCodeAt(0))}
export async function encodeProject(data){const raw=new TextEncoder().encode(JSON.stringify(data));if("CompressionStream"in window){const cs=new CompressionStream("gzip"),buf=await new Response(new Blob([raw]).stream().pipeThrough(cs)).arrayBuffer();return"gz."+bytesToB64(new Uint8Array(buf))}return"raw."+bytesToB64(raw)}
export async function decodeProject(hash){const [mode,payload]=hash.split(".",2);if(!payload)return null;let bytes=b64ToBytes(payload);if(mode==="gz"&&"DecompressionStream"in window){const ds=new DecompressionStream("gzip");bytes=new Uint8Array(await new Response(new Blob([bytes]).stream().pipeThrough(ds)).arrayBuffer())}return JSON.parse(new TextDecoder().decode(bytes))}
